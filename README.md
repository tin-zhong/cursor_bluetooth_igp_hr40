# HR40 Offline Fitness

一款面向 iGPSPORT/IGP HR40 心率带的原生 Android 离线运动监测应用。应用运行在支持 BLE 的 Android 设备上（例如一加平板 2 Pro），通过标准 Bluetooth Heart Rate Service 连接心率带，并在本机完成资料存储、运动记录、统计计算和 PDF 报告导出。

## 功能

- 首次打开应用录入运动人员姓名、性别、年龄、身高、体重，数据保存在设备本地应用私有存储中。
- 扫描并连接 HR40 心率带，订阅标准心率测量特征 `0x2A37`。
- 从“开始运动”到“结束运动”持续记录心率采样，运动中数据先写入 JSON 缓冲，结束运动后归档到 Room 数据库（SQLite）。
- 离线计算平均/最高/最低心率、心率区间、估算能量消耗和采样数量。
- 运动结束后导出 PDF 报告，包含运动概览、心率曲线、消耗和心率区间图表。
- 应用内「查看运动明细」：选择历史记录即可查看心率曲线、消耗、心率区间与力量训练组数，无需导出 PDF。
- 「训练倒计时」：设定目标时间，到时播放提示音并语音播报，适用于平板支撑等计时训练。
- PDF 写入设备 `Downloads/HR40` 目录，并可通过系统分享面板发送。

## 构建

项目是无第三方运行时依赖的原生 Android/Java 应用，可用 Android Studio 打开根目录后同步并运行，也可以直接使用仓库内的 Gradle Wrapper 构建。

```bash
./scripts/setup_android_env.sh
export ANDROID_HOME="$HOME/android-sdk"
export ANDROID_SDK_ROOT="$HOME/android-sdk"
export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH"
./scripts/build_dist_apk.sh
```

### 统一签名（v3.5.0 起，重要）

为避免不同构建机使用各自 `debug.keystore` 导致**签名冲突、无法覆盖安装**，仓库在 `cursor_bluetooth_igp_hr40/keystore/hr40-distribution.keystore` 中提交了**唯一**的分发用二进制密钥库。

**之后所有新版本的 `dist/` 安装包都必须通过该文件签名生成**（执行 `./scripts/build_dist_apk.sh`），不得改用本机 `~/.android/debug.keystore` 或其它临时证书。详见 `keystore/README.md`、`dist/SIGNING.md`。

```bash
./scripts/check_keystore_matches_distribution.sh   # 确认密钥库与仓库一致
```

### 分发策略（debug 优先）

当前阶段 **Offline / Online 均只发布 debug 包**，待功能验收通过后再生成正式版 `v{version}.apk`。

| 类型 | 路径 | 说明 |
|------|------|------|
| **测试用（当前推荐）** | `dist/hr40-offline-fitness-debug.apk` | 构建脚本默认只更新此文件 |
| 正式版（暂缓） | `dist/hr40-offline-fitness-v{version}.apk` | 需显式执行 `RELEASE_DIST_APK=1 ./scripts/build_dist_apk.sh` |

为了便于平板测试，请安装 `dist/` 中最新 **debug** APK：

```text
dist/hr40-offline-fitness-debug.apk
```

下载：仓库 `main` 分支 → `dist/hr40-offline-fitness-debug.apk` → **Download raw file**。

## v3.6.0 更新

- **数据上传按钮（Online）**：主页新增「数据上传」按钮，点击后检测云端已有哪些运动记录，仅重新上传缺失的数据；当本地数据全部已在云端时按钮显示为绿色背景
- **修复心率时长被截断**：运动过程中心率带若断开（不仅限于「连接异常: 8」），现在会自动重新扫描连接，避免心率采样中途中断导致显示的心率时长远小于真实训练时长
- **开启新运动时动态清空缓存**：开始新运动前清空上一段运动遗留的缓存 JSON 与心率状态（最近心率、重连标记、心率显示），防止历史心率值混入新运动造成开局剧烈波动
- **消耗估算更准确**：能量与心率区间统计在样本间隔过大（暂停 / 断连 / 运动切换，>10 秒）时跳过该区间，不再把空档时间计入消耗或区间时长；Web 端同步该逻辑保持一致

## v3.5.1 更新

- 训练倒计时：目标时间改为与力量训练次数相同的累加/累减按钮（`-10/-5/-1`、`+1/+5/+10`），取消快捷预设
- 「训练倒计时」仅在运动中显示（有氧、力量均可），主页面不再显示该入口

## v3.5.0 更新

- **新分发签名线**：提交固定密钥库 `keystore/hr40-distribution.keystore`，构建脚本强制校验证书指纹，杜绝再发布“同版本号、不同签名”的包
- 包含 v3.10.0 功能：应用内「查看运动明细」、 「训练倒计时」
- **升级说明**：若已安装 v3.4.8、v3.10.0 等旧签名线的 APK，请先**卸载**再安装 v3.5.0（签名不同，无法直接覆盖）。卸载前请按需导出 PDF/JSON 备份

## v3.10.0 更新（旧签名线，已由 v3.5.0 取代分发）

- 新增「查看运动明细」与「训练倒计时」
- 注意：v3.10.0 使用旧 debug 证书线，与 v3.5.0 **不可**互相覆盖安装

## v3.4.8 更新

- 力量训练录组时重量允许为 0（界面与 v3.4.7 一致）

## v3.4.7 更新

- 优化运动时长显示：与手机系统时钟整秒同步跳变，刷新时刻对齐下一秒的 0 毫秒
- 与心率回调解耦；历史数据管理列表显示修复
- **请勿安装** `dist/hr40-offline-fitness-v3.4.6.apk`（签名与 v3.4.5 不一致）

## v3.4.6 更新

- 修复历史数据管理对话框中训练记录列表不显示的问题（避免 `setMessage` 与多选列表冲突）
- 历史列表与「导出运动记录 PDF」使用相同的可导出记录范围
- 注意：已发布的 v3.4.6 APK 签名与 v3.4.5 不一致，请改用 v3.4.7

## v3.4.2 更新

- 历史数据管理展示历史记录并支持批量多选清理
- 去掉“立即清理旧数据”入口，改为批量选择后清理
- 自动清理状态与保留周期在同一配置策略中展示

## v3.4.1 更新

- 导出文件管理升级为批量多选删除/移动
- 修复“历史数据管理”点击后闪退问题（重构设置交互逻辑）

## v3.4.0 更新

- 新增导出文件管理：可对已导出的 PDF/JSON 文件进行分享、删除、移动到归档目录
- 原始训练数据(JSON)导出支持选择具体训练记录（不再只导出整体）
- 新增历史数据管理：支持手动清理 + 自动定时清理（可配置保留周期）

## v3.3.2 更新

- 运动进行中仅显示三个主按钮：扫描并连接HR40、开始/暂停运动、结束运动
- 导出、动作管理、编辑资料按钮在运动中自动隐藏

## v3.3.1 更新

- 结束运动按钮在可点击时显示为红色高亮
- 点击结束运动先弹出确认对话框，确认后才结束，防止误操作

## v3.3.0 更新

- 新增运动暂停功能：开始后按钮切换为“暂停运动”，暂停后可“继续运动”
- 暂停期间不累计运动时长，不写入心率采样

## v3.2.1 更新

- 运动中实时写入 JSON 缓冲；结束运动后自动写入 Room 数据库并清理对应 JSON 文件

## v3.2.0 更新

- 训练记录改为：运动中 JSON + 结束后 Room 归档
- 首次启动自动把遗留 JSON 训练记录迁移进数据库

## v3.1.0 更新

- 每周一首次打开 App 强制更新体重，用于新一周消耗估算
- 引入 Room 本地数据库，训练结束后写入一条结构化记录（含心率采样与力量组）
- 新增“导出原始训练数据(JSON)”按钮，可分享数据库原始数据

## v3.0.0 更新

- 消耗算法新增“力量模式修正系数”，力量训练按 `0.88` 系数衰减（有氧模式保持原算法）

## v2.2.0 更新

- 力量训练 PDF 导出支持完整分页，不再只显示前 9 条
- 主界面心率与时长固定在顶部，避免滚动导致看不到关键数据
- “结束运动”与“导出 PDF”分离，支持选择任意历史记录导出

## v2.1.4 更新

- 心率带断开、扫描或连接中时，心率显示 `--` 而非保留上次数值
- 增加顶部系统栏安全间距，标题不再紧贴状态栏

## v2.1.3 更新

- 升级心率消耗估算算法（Keytel 公式 + 60 bpm 阈值 + 区间平均）
- 运动中实时显示累计估算消耗
- 简洁主页面，开始运动后选择力量/有氧训练
- 动作管理支持添加和删除
- Material Components 原生界面美化

## 版本号规则

版本号格式为 `x.y.z`：

- `x`：消耗算法升级或演进时 +1
- `y`：新增功能时 +1
- `z`：仅页面/UI 调整时 +1

当前版本：`3.5.1`
- `x=3`：Keytel 算法增加“力量模式 0.88 修正系数”
- `y=5`：固定仓库密钥库 + 统一分发签名（含 v3.10.0 功能）
- `z=1`：训练倒计时入口与设定交互调整

## 安装与升级

- **新安装 / 当前推荐**：`dist/hr40-offline-fitness-debug.apk`（与正式包同签名，见 `dist/SIGNING.md`）
- **正式版（暂缓发布）**：`dist/hr40-offline-fitness-v3.5.1.apk`（签名 SHA-256：`7ca2098f…`）
- 从 **v3.5.0** 升级：直接覆盖安装 v3.5.1（同签名线）
- 从 **v3.4.8、v3.10.0** 等旧包升级：先卸载旧版，再安装 v3.5.1（签名已更换，无法覆盖安装）
- 从 **v3.4.5** 线升级：同样需卸载后安装 v3.5.1
- 维护者发布新版本：仅使用 `./scripts/build_dist_apk.sh` + 仓库内 `keystore/hr40-distribution.keystore`
- 未开始运动时标题显示 **HR40 离线运动监测 v3.5.1**

## 权限

- Android 12+：`BLUETOOTH_SCAN`、`BLUETOOTH_CONNECT`
- Android 11 及以下：`ACCESS_FINE_LOCATION`（BLE 扫描要求）

所有运动和个人资料数据均保存在本机，不依赖网络服务。
