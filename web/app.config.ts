export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      neutral: 'slate',
    },
    button: {
      slots: {
        base: 'justify-center text-center',
      },
    },
  },
});
